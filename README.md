# MicroClimate

## 📁 Clone the Repository

```bash
git clone https://github.com/your-username/occupancy-analysis.git
cd occupancy-analysis
```

## 📦 Install Python Libraries

Make sure you have Python 3.8+ installed. Then set up your environment:

```bash
python -m venv venv
source venv/bin/activate  # On Windows: venv\Scripts\activate
pip install -r requirements.txt
```

## 🧩 Recommended VS Code Extensions

Install the following extensions for the best notebook experience in [Visual Studio Code](https://code.visualstudio.com/):

- ✅ Jupyter
- ✅ Jupyter Keymap
- ✅ Jupyter Notebook Renderers
- ✅ Jupyter Cell Tags
- ⏱️ Python

## ▶️ Run the Notebook

Open `analysis.ipynb` and run the cells step by step or click "Run all".

## 🔁 Pull & Push to GitHub

Before making changes, make sure your local repo is up to date:

```bash
git pull origin main
```

After edits:

```bash
git add .
git commit -m "Updated analysis and notebook structure"
git push origin main
```
